on:
  workflow_call:
    outputs:
      name:
        description: >-
          Name of the Python package as determined from pyproject.toml
        value: ${{ jobs.package-name.outputs.name }}

jobs:
  package-name:
    if: startsWith(github.ref, 'refs/tags')
    name: Determine package name
    runs-on: ubuntu-slim
    outputs:
      name: ${{ steps.package-name.outputs.name }}
    steps:
      - uses: ComPWA/actions/get-pypi-name@99e91bcc486218c7dbbb4f2e0319bfcb028562aa # 4.0.1
        id: package-name
