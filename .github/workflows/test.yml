env:
  FORCE_COLOR: "1"
  PYTHONHASHSEED: "0"

on:
  workflow_call:
    inputs:
      coverage-python-version:
        default: ""
        required: false
        type: string
      macos-python-version:
        required: false
        type: string
      multithreaded:
        default: true
        required: false
        type: boolean
      skipped-python-versions:
        required: false
        type: string
      test-output-path:
        required: false
        type: string
    secrets:
      CODECOV_TOKEN:
        description: Token for uploading reports to Codecov. See https://docs.codecov.com/docs/codecov-uploader.
        required: false

jobs:
  create-matrix:
    name: Determine job matrix
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    runs-on: ubuntu-slim
    steps:
      - id: set-matrix
        uses: ComPWA/actions/create-pytest-matrix@99e91bcc486218c7dbbb4f2e0319bfcb028562aa # 4.0.1
        with:
          coverage-python-version: ${{ inputs.coverage-python-version }}
          macos-python-version: ${{ inputs.macos-python-version }}
          skipped-python-versions: ${{ inputs.skipped-python-versions }}

  test:
    name: Run unit tests
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      UV_PYTHON: ${{ matrix.python-version }}
    needs: create-matrix
    runs-on: ${{ matrix.runs-on }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.create-matrix.outputs.matrix) }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          cache-dependency-glob: |
            **/pyproject.toml
            **/uv.lock
          cache-suffix: test-${{ matrix.python-version }}
      - uses: ComPWA/actions/cache-sympy@99e91bcc486218c7dbbb4f2e0319bfcb028562aa # 4.0.1
      - if: inputs.multithreaded
        name: Activate pytest multithreading
        run: echo "PYTEST_XDIST_WORKER_COUNT=auto" | tee -a $GITHUB_ENV
      - id: task-runner
        uses: ComPWA/actions/determine-task-runner@99e91bcc486218c7dbbb4f2e0319bfcb028562aa # 4.0.1
      - uses: prefix-dev/setup-pixi@d3f436a425481402e6a95a1d1fc10331c708cd9e # v0.10.2
        if: steps.task-runner.outputs.value == 'pixi'
        with:
          cache: true
      - if: matrix.codecov
        run: |-
          ${{ case(
            steps.task-runner.outputs.value == 'pixi',
            'pixi run cov',
            'uvx --from poethepoet poe cov'
          ) }}
      - if: ${{ !matrix.codecov }}
        run: |-
          ${{ case(
            steps.task-runner.outputs.value == 'pixi',
            'pixi run test',
            'uvx --from poethepoet poe test'
          ) }}
      - if: matrix.codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: ./coverage.xml
          flags: unittests
          token: ${{ secrets.CODECOV_TOKEN }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: inputs.test-output-path
        with:
          name: test-output-${{ matrix.runs-on }}-${{ matrix.python-version }}
          path: ${{ inputs.test-output-path }}
